# Archived documents

Superseded, kept for history. **Do not treat as current** — each is replaced by the document named
below. Content that was still live has been folded into the replacement, not dropped.

| File | Superseded by | Why |
|---|---|---|
| `roadmap.md` (07-23) | `autonav_reference.md` §1–2, §12 · `autonomy_plan.md` | It called itself the single source of truth for direction. It also uses **different L-numbers from `autonomy_plan.md` (08-02)**, which made mixing the two an active hazard. |
| `yaw_tuning_session.md` (07-29) | `rover_yaw_response.md` · `setup_manual.md` §A7 | A floor-session checklist for todo #20, which is solved. The findings and the parameter changes it led to are in the replacements. |
| `third_party.md` | `setup_manual.md` §B2 | Folded in whole — gitignored sources, their pins, and the Orbbec patch that a re-clone silently loses. |
